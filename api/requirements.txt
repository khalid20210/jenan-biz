# ══════════════════════════════════════════════════════════════════
#  جنان بيز — requirements.txt الشامل
#  يغطي: الأمان · الأداء · الاحترافية · الدقة · التصميم · جميع الأقسام
# ══════════════════════════════════════════════════════════════════

# ─── إطار العمل والخادم ─────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.29.0       # WebSockets + HTTP/1.1 محسّن
gunicorn>=22.0.0                 # WSGI لبيئة الإنتاج
starlette>=0.37.0                # أساس FastAPI (middleware متقدم)

# ─── الأداء والسرعة ──────────────────────────────────────────────
orjson>=3.9.0                    # JSON أسرع 10x من المكتبة القياسية
brotli>=1.1.0                    # ضغط HTTP Brotli (أفضل من gzip)
aiocache[redis]>=0.12.0          # تخزين مؤقت Async مع Redis
cachetools>=5.3.0                # تخزين مؤقت في الذاكرة LRU/TTL
httpx[http2]>=0.27.0             # HTTP/2 للطلبات الخارجية (Moyasar/OpenAI)
aiofiles>=23.2.0                 # قراءة/كتابة ملفات Async
tenacity>=8.2.0                  # إعادة المحاولة التلقائية عند فشل API

# ─── الأمان والمصادقة ────────────────────────────────────────────
PyJWT>=2.8.0                     # توقيع وتحقق JWT
cryptography>=42.0.0             # تشفير متقدم (AES, RSA, HMAC)
passlib[bcrypt]>=1.7.4           # تجزئة كلمات المرور bcrypt
itsdangerous>=2.1.0              # رموز آمنة موقّعة (reset password / email verify)
pyotp>=2.9.0                     # TOTP لمصادقة ثنائية (2FA)
bleach>=6.1.0                    # تنظيف HTML ومنع XSS
python-multipart>=0.0.9          # رفع الملفات بأمان
python-magic>=0.4.27             # كشف نوع الملف الحقيقي (ليس فقط الامتداد)

# ─── التحقق من البيانات ──────────────────────────────────────────
pydantic>=2.0.0
pydantic[email]>=2.0.0
email-validator>=2.1.0
phonenumbers>=8.13.0             # التحقق من أرقام الجوال السعودية
python-stdnum>=1.20              # التحقق من أرقام السجل التجاري / الوطني

# ─── الإعدادات والمتغيرات ────────────────────────────────────────
python-dotenv>=1.0.0

# ─── الذكاء الاصطناعي ────────────────────────────────────────────
openai>=1.30.0                   # GPT-4o-mini: التحليل، الدراسات، الأكاديمية

# ─── قاعدة البيانات والاتصال ─────────────────────────────────────
supabase>=2.4.0                  # Supabase Python SDK الرسمي
psycopg2-binary>=2.9.0           # اتصال مباشر بـ PostgreSQL (تشغيل SQL migrations)

# ─── الأداء والطوابير والحد من الطلبات ───────────────────────────
redis>=5.0.0                     # تخزين مؤقت + rate limiting
slowapi>=0.1.9                   # Rate limiting IP-based لـ FastAPI
celery[redis]>=5.3.0             # طوابير المهام الثقيلة (PDF، بريد)

# ─── البريد الإلكتروني ───────────────────────────────────────────
aiosmtplib>=3.0.0                # SMTP Async (لا يجمّد الخادم)

# ─── توليد PDF احترافي ───────────────────────────────────────────
reportlab>=4.0.0                 # PDF برمجي (شهادات، تقارير هيكلية)
xhtml2pdf>=0.2.17                # HTML→PDF بدون GTK (Windows آمن)
arabic-reshaper>=3.0.0           # دعم تشكيل العربية في PDF
python-bidi>=0.6.7               # دعم RTL في PDF
Pillow>=10.3.0                   # معالجة الصور والشعارات
qrcode[pil]>=7.4.0               # QR code للشهادات والتحقق
python-docx>=1.1.0               # توليد ملفات Word (.docx)
svglib>=1.5.1                    # تحويل SVG→PDF (استوديو التصميم)
rlPyCairo>=0.3.0                 # دعم Cairo لـ reportlab (اختياري)

# ─── التصميم ومعالجة الصور ───────────────────────────────────────
colorthief>=0.2.1                # استخراج لوحة الألوان من الصور
Wand>=0.6.13                     # ImageMagick binding (تحويل الصور)

# ─── الحسابات المالية والبيانات ──────────────────────────────────
pandas>=2.2.0                    # تحليل البيانات المالية
numpy>=1.26.0                    # الحسابات العلمية والإحصائية
openpyxl>=3.1.0                  # قراءة/كتابة Excel
Babel>=2.14.0                    # تنسيق العملات والأرقام (ريال سعودي)
scipy>=1.13.0                    # التحليل الإحصائي المتقدم

# ─── معالجة النصوص والقوالب ──────────────────────────────────────
markdown>=3.6.0                  # تحويل Markdown→HTML
jinja2>=3.1.0                    # قوالب HTML للبريد والتقارير
beautifulsoup4>=4.12.0           # تحليل HTML/XML
lxml>=5.2.0                      # معالجة XML سريعة (يدعم xhtml2pdf)
chardet>=5.2.0                   # كشف ترميز النصوص

# ─── الوقت والتواريخ ─────────────────────────────────────────────
python-dateutil>=2.9.0           # معالجة التواريخ المتقدمة
pytz>=2024.1                     # منطقة زمنية (Asia/Riyadh)
hijri-converter>=2.3.1           # تحويل التاريخ الميلادي↔الهجري

# ─── المراقبة وتتبع الأخطاء ──────────────────────────────────────
loguru>=0.7.0                    # سجلات احترافية ملوّنة
sentry-sdk[fastapi]>=2.0.0      # مراقبة الأخطاء في الإنتاج
prometheus-client>=0.20.0        # مقاييس الأداء (Metrics)

# ─── الاختبار والجودة ────────────────────────────────────────────
pytest>=8.0.0                    # إطار الاختبار
pytest-asyncio>=0.23.0           # اختبار الدوال Async
pytest-cov>=5.0.0                # قياس تغطية الكود
httpx>=0.27.0                    # عميل HTTP للاختبار
